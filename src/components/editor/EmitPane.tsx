import { Pane } from './Pane'

// Sub-step 4 fills this in (live artifact + copy + tabs). Stub for the shell.
export function EmitPane() {
  return (
    <Pane code="CH·03" title="Emit">
      <p className="font-mono text-xs text-ink-faint">live artifact — coming next</p>
    </Pane>
  )
}
